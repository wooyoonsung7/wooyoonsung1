using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class 우윤성_2024137035_4 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        string[] array = { "감자", "고구마", "토마토", "가지" };
        Debug.Log(string.Join(",", array));
    }
}
